# Fechatter Frontend - Production Configuration
# 生产环境：Vercel部署直连后端服务

app:
  name: "Fechatter"
  version: "1.0.0"
  environment: "production"
  debug: false

# 前端服务器配置（生产环境由Vercel管理）
frontend:
  port: 3000
  host: "0.0.0.0"

# API配置 - 生产环境通过ngrok HTTPS隧道
api:
  # 🚀 NGROK隧道地址 - 从ngrok输出复制URL替换下面的地址
  gateway_url: "https://example-ngrok-url.ngrok.io"

  # 生产环境使用ngrok隧道
  base_url: "https://example-ngrok-url.ngrok.io/api"
  file_url: "https://example-ngrok-url.ngrok.io/files"
  sse_url: "https://example-ngrok-url.ngrok.io/events"
  notify_url: "https://example-ngrok-url.ngrok.io/notify"

  # 请求配置
  timeout: 15000
  max_retries: 3
  retry_delay: 1000

# CORS配置 - 生产环境
cors:
  enabled: true
  origins:
  - "https://fechatter-frontend-kto9xf8u5-zhkevin46-gmailcoms-projects.vercel.app" # 最新部署域名
  - "https://fechatter-frontend-j19109swu-zhkevin46-gmailcoms-projects.vercel.app"
  - "https://fechatter-frontend-cahw21vjh-zhkevin46-gmailcoms-projects.vercel.app"
  - "https://fechatter-frontend-h00f7pv8z-zhkevin46-gmailcoms-projects.vercel.app"
  - "https://*.vercel.app" # 所有Vercel部署域名
  - "https://fechatter.vercel.app"
  credentials: true

# 开发工具（生产环境禁用）
dev_tools:
  enable_devtools: false
  enable_source_maps: false
  hot_reload: false

# 日志配置
logging:
  level: "warn"
  enable_network_logs: false
  enable_api_logs: false

# 测试账户配置 - 生产环境也保留用于演示
test_accounts:
- email: "test@fechatter.com"
  password: "test123"
  name: "Test User"
  description: "演示账户"
- email: "dev@fechatter.com"
  password: "dev123"
  name: "Dev User"
  description: "开发演示账户"
- email: "admin@fechatter.com"
  password: "admin123"
  name: "Admin User"
  description: "管理员演示账户"
