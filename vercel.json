{
  "buildCommand": "yarn generate-proto && yarn copy-configs && yarn vercel-build",
  "outputDirectory": "dist",
  "cleanUrls": false,
  "rewrites": [
    {
      "source": "/((?!config/|favicon|manifest|.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
