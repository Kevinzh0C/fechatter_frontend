# 前端问题与改进清单

## 🔴 需要立即修复的问题

### 1. **状态管理错误**
- ✅ **已修复**: `chat.js` 中 `hiddenDMs` 未定义 - 已添加到 state (第82行)
- **待修复**: `pendingOptimisticMessages` Map 可能导致内存泄漏
  - 位置：`stores/chat.js` 第75行
  - 建议：添加定期清理机制或使用 WeakMap

### 2. **API 错误处理不当**
- **问题**: 多处 catch 块只是 console 输出错误，没有用户友好提示
- **位置**: 
  - `stores/auth.js`: 10+ 处 console.error
  - `stores/chat.js`: 7+ 处 console.error
  - `stores/workspace.js`: 多处错误未处理
- **建议**: 统一使用 `useNotifications` 显示用户友好的错误提示

### 3. **未读消息计数问题**
- **问题**: `/unread-counts` API 可能返回 404
- **位置**: `stores/chat.js` 第1575, 1690行
- **建议**: 添加错误处理，API 不存在时降级处理

## 🟡 功能不完整或需要优化

### 1. **实时功能**
- **SSE 连接管理**: 
  - 位置：`services/sse.js`
  - 问题：长时间连接可能中断，重连机制需要优化
  - 建议：实现心跳检测和智能重连

- **在线用户状态**:
  - 位置：`components/chat/OnlineUsers.vue`
  - 问题：目前显示所有用户为在线状态（第130行）
  - 建议：集成真实的在线状态追踪

- **输入状态指示**:
  - 位置：`components/chat/TypingIndicator.vue`
  - 状态：功能已实现但需要测试

### 2. **搜索功能**
- **搜索历史**: 后端API未实现
- **高级搜索**: 时间范围、消息类型过滤未实现
- **搜索建议**: API已实现但前端未充分利用

### 3. **文件处理**
- **拖放预览**: 低优先级待实现
- **文件类型图标**: 不同文件类型显示不同图标
- **大文件上传进度**: 已实现但可以优化UI

### 4. **工作空间功能**
- **邀请用户**: 后端 API 未实现（`stores/workspace.js` 第120行）
- **删除成员**: 后端 API 未实现（`stores/workspace.js` 第147行）

## 🟢 性能优化建议

### 1. **缓存优化**
- **消息缓存**: 已实现但可以优化过期策略
- **聊天列表缓存**: 10分钟过期可能太长
- **搜索结果缓存**: 未实现

### 2. **内存管理**
- **清理调试日志**: 生产环境应移除大量 console.log
- **事件监听器清理**: 确保组件销毁时清理所有监听器
- **大数据集优化**: 消息列表虚拟滚动已实现但可以进一步优化

### 3. **加载性能**
- **预加载优化**: `auth.js` 中的预加载可能过于激进
- **按需加载**: 某些大组件可以实现懒加载
- **图片加载**: 已使用 lazy loading 但可以添加占位符

## 🔵 用户体验改进

### 1. **错误提示**
- 统一错误提示样式和位置
- 添加重试机制的用户界面
- 网络断开时的明确提示

### 2. **加载状态**
- 统一加载动画样式
- 添加骨架屏提升感知性能
- 长时间加载的进度提示

### 3. **移动端适配**
- 响应式布局需要进一步优化
- 触摸手势支持（滑动操作等）
- 移动端专属UI优化

### 4. **快捷键支持**
- 搜索快捷键 (Ctrl+F) 已实现
- 可以添加更多快捷键：
  - 新建聊天 (Ctrl+N)
  - 切换聊天 (Ctrl+Tab)
  - 发送文件 (Ctrl+U)

## 📋 TODO 列表汇总

### 高优先级
1. 修复 `pendingOptimisticMessages` 内存泄漏风险
2. 完善错误处理机制，添加用户友好提示
3. 修复未读消息计数 API 错误处理
4. 优化 SSE 重连机制

### 中优先级
1. 实现真实的在线用户状态
2. 完善搜索功能（历史、高级搜索）
3. 优化文件上传体验
4. 清理生产环境的调试日志

### 低优先级
1. 实现搜索结果缓存
2. 添加更多快捷键支持
3. 优化移动端体验
4. 实现文件拖放预览

## 🛠️ 建议的修复顺序

1. **第一阶段**：修复关键错误
   - 内存泄漏问题
   - API 错误处理
   - 未读消息功能

2. **第二阶段**：完善核心功能
   - 实时功能优化
   - 搜索功能完善
   - 文件处理改进

3. **第三阶段**：性能和体验优化
   - 缓存策略优化
   - 移动端适配
   - 快捷键和辅助功能